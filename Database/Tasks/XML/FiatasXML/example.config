# Configuration file for the Fiatas anatomy XML file generator.  
# This generates the old format.
#
# File format rules
# 1. Blank lines and lines that start with "#" or " " are ignored.
# 2. Lines that set parameters have
#    o Parameter name, starting in first column
#    o Followed by a space, an equals sign, and another space,
#    o followed by the value to assign to that space.
#

# Database connection paramaters for existing anatomy database.
DB_HOST = localhost
DB_DATABASE = anatomy4
DB_USER = davidc
DB_PASSWORD = 

# Where do you want the output files to go?
# Directory must already exist.
OUTPUT_DIRECTORY = ../../../Versions/Version004/Formats/Deprecated/FiatasXML

# How to handle groups?  Possibilites are
# EXCLUDE: don't show them at all
# EXPAND:  Show them and expand down to all their children.
# Other (unimplemented) possiblities might be:
#  Show only the group term, but no kids 
#  Show the first layer of kids, but nothing below that.
GROUPS = EXCLUDE

# Is debugging on or off?  Valid values are ON or OFF
DEBUGGING = OFF
